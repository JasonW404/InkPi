[project]
name = "einkdashboard"
version = "0.1.0"
description = "Raspberry Pi e-ink dashboard with scheduled partial/full refresh"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
	"Pillow>=10.0.0",
	"requests>=2.32.0",
]
